exec docker-compose up
open http://{rpi ip}/

asp.net is slow on first access

mariadb
  user: root/heslo
	host: mariadb 

ttrss config
  open adminer
  create new database for tt-rss
  open tt-rss, it starts with installer
  save config.php in ./ttrss/data
  restart ttrss container to feed update reads saved config
  default user is admin/password

